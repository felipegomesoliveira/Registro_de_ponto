{
    "sqltools.connections": [
        {
            "previewLimit": 50,
            "driver": "SQLite",
            "name": "brainny",
            "database": "${workspaceFolder:project}/data/brainny.db"
        }
    ]
}